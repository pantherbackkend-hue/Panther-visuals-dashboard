<%- include("../../partials/header", { pageTitle }) %>
<%- include("../partials/layout-start", { activeSection }) %>

<section class="admin-page-head">
  <div>
    <p class="eyebrow">Workspace management</p>
    <h1>Workspaces</h1>
    <p class="muted">Create, disable, edit, and delete workspaces from the browser.</p>
  </div>
  <a class="btn" href="/admin/shops/new">New workspace</a>
</section>

<div class="card admin-panel">
  <% if (!shops.length) { %>
    <p class="muted">No workspaces available.</p>
  <% } else { %>
    <div style="overflow-x: auto;">
      <table class="table admin-table">
        <thead>
          <tr>
            <th>Workspace</th>
            <th>Slug</th>
            <th>Status</th>
            <th>Editor</th>
            <th>Total Projects</th>
            <th style="width: 1%; white-space: nowrap;">Actions</th>
          </tr>
        </thead>
        <tbody>
          <% shops.forEach((shop) => { %>
            <tr>
              <td>
                <strong><%= shop.name %></strong>
                <% if (shop.description) { %>
                  <div class="muted small"><%= shop.description %></div>
                <% } %>
              </td>
              <td class="mono"><%= shop.slug %></td>
              <td>
                <span class="tag tag--<%= shop.isActive === false ? "danger" : shop.isOpen === false ? "warn" : "ok" %>">
                  <%= shop.statusLabel %>
                </span>
              </td>
              <td><%= shop.assignedVendorName %></td>
              <td><%= shop.totalOrders %></td>
              <td>
                <div class="admin-actions">
                  <a class="btn btn--small btn--outline" href="/admin/shops/<%= shop._id %>">View</a>
                  <a class="btn btn--small btn--outline" href="/admin/shops/<%= shop._id %>/edit">Edit</a>
                  <form method="post" action="/admin/shops/<%= shop._id %>/toggle" class="inline" onsubmit="return confirm('Toggle this workspace status?')">
                    <button class="btn btn--small btn--ghost" type="submit"><%= shop.isActive ? "Disable" : "Enable" %></button>
                  </form>
                  <form method="post" action="/admin/shops/<%= shop._id %>/delete" class="inline" onsubmit="return confirm('Delete this workspace? This also removes its assets.')">
                    <button class="btn btn--small btn--danger" type="submit">Delete</button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>

<%- include("../partials/layout-end") %>
<%- include("../../partials/footer") %>
